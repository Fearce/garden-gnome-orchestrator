{
  "name": "claude-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "A director's console for running Claude Code agents — enrich, plan, research, dispatch Opus implementors, inject mid-work.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "dev": "concurrently --raw \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web",
    "serve": "concurrently --raw \"npm:serve:server\" \"npm:dev:web\"",
    "serve:server": "npm run serve --prefix server",
    "build": "npm run build --prefix web && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix web",
    "health": "npm run health --prefix server",
    "quality": "npm run health && npm run typecheck && npm run test:gates --prefix server && npm run probe:run-errors --prefix server && npm run probe:parks --prefix server && npm run probe:accounts --prefix server && npm run probe:console && npm run probe:chips && npm run audit:deps --prefix server && npm run audit:secrets --prefix server && npm run probe:db-size --prefix server",
    "probe:console": "node web/scripts/console-smoke.cjs",
    "probe:chips": "node web/scripts/check-accounts-visible.cjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
